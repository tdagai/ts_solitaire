import { inspect } from 'util';
import {
  rl,
  question,
  initiateGame,
} from './helpers';

console.log(inspect(initiateGame(), false, null, true));

// const playGame = async () => {

// }

// playGame();